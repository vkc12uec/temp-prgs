
/*
  max area under histogram

*/

#include "topcoder_header.h"

int main () {
  int h[] = {9, 5, 2 , 1, 3, 5, 4, 8, 2, 7};
  int len = SizeOfArray(h);

  stack <int> grad;

  for (int i=0; i<len; i++)

  return 0;
}
